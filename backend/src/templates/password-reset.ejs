<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Reset Your Password - DropSecure</title>
  <style>
    body { font-family: 'Segoe UI', Arial, sans-serif; background: #fff8f2; color: #333; }
    .header { background: linear-gradient(90deg, #800020 60%, #D4AF37 100%); color: #fff; padding: 24px 0; text-align: center; }
    .logo { font-size: 2rem; font-weight: bold; letter-spacing: 2px; }
    .content { background: #fff; border-radius: 12px; margin: 32px auto; max-width: 480px; padding: 32px; box-shadow: 0 2px 12px #80002022; }
    .footer { color: #800020; text-align: center; margin-top: 32px; font-size: 0.95em; }
    .code { font-size: 1.3em; font-weight: bold; letter-spacing: 2px; color: #D4AF37; background: #80002010; padding: 8px 18px; border-radius: 6px; margin: 8px 0; display: inline-block; }
  </style>
</head>
<body>
  <div class="header">
    <span class="logo">DropSecure</span>
  </div>
  <div class="content">
    <h2 style="color:#800020;">Password Reset Request</h2>
    <p>Hello <%= name %>,</p>
    <p>
      We received a request to reset your DropSecure account password.<br>
      Please use the code below to reset your password.<br>
      This code will expire in <b><%= expiresIn %></b>.
    </p>
    <% if (token) { %>
      <div>Your password reset code:</div>
      <div class="code"><%= token %></div>
    <% } %>
    <p style="margin-top:18px;">
      If you did not request this, you can safely ignore this email.
    </p>
  </div>
  <div class="footer">
    &copy; <%= new Date().getFullYear() %> DropSecure. All rights reserved.
  </div>
</body>
</html>